Just having fun with a customized and sassy LLM which converts from Python to C++ and writes its context to a json file which acts as a "memory". The idea is also for it to summerize the current context file into more general points (thoughts or somethin') which then replaces the previous context. Work in progress AF!
